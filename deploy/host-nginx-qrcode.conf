server {
    listen 80;
    server_name qrcode.tecso.team;

    location / {
        # Forward the original request URI to the backend so paths like
        # /tecsoqr/assets/... are preserved and not doubled.
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Let certbot verify
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
}

# Example TLS config for /etc/nginx/sites-available/qrcode.tecso.team
#
# server {
#   listen 443 ssl http2;
#   server_name qrcode.tecso.team;
#
#   ssl_certificate /etc/letsencrypt/live/qrcode.tecso.team/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/qrcode.tecso.team/privkey.pem;
#
#   location / {
#       proxy_pass http://127.0.0.1:3001/tecsoqr/;
#       proxy_set_header Host $host;
#       proxy_set_header X-Real-IP $remote_addr;
#       proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#       proxy_set_header X-Forwarded-Proto $scheme;
#   }
# }
